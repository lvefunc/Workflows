( function () {
    mw.workflows.ui.Renderer = function MwWorkflowsUiRenderer() {
    };

    OO.initClass( mw.workflows.ui.Renderer );

    mw.workflows.ui.Renderer.prototype.load = function ( ordering, from, limit ) {
    };
}() );